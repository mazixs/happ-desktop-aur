# 🚀 Быстрый старт Happ Desktop

## Установка (3 команды)

```bash
git clone https://github.com/mazixs/happ-desktop-aur.git
cd happ-desktop-aur
makepkg -si
```

## Проверка

```bash
./test-capabilities.sh
```

Ожидаемый вывод:
```
✓ Пакет happ-desktop установлен
✓ GUI приложение: /opt/happ/Happ
✓ VPN ядро sing-box: /opt/happ/tun/sing-box
✓ VPN ядро xray: /opt/happ/core/xray
✓ /opt/happ/tun/sing-box cap_net_admin=ep
✓ /opt/happ/core/xray cap_net_admin=ep
✓ Happ Desktop готов к работе!
```

## Запуск

```bash
happ
```

Или через меню: **Меню приложений → Utility → Happ**

## Основные файлы

| Файл | Назначение |
|------|------------|
| `PKGBUILD` | Сборочный скрипт |
| `happ-desktop.install` | Установка capabilities |
| `README.md` | Полная документация |
| `BUILD.md` | Инструкция по сборке |
| `CAPABILITIES.md` | Детали про безопасность |
| `test-capabilities.sh` | Проверка установки |

## Проверка capabilities

```bash
# Проверить sing-box
getcap /opt/happ/tun/sing-box
# Ожидается: cap_net_admin=ep

# Проверить xray
getcap /opt/happ/core/xray
# Ожидается: cap_net_admin=ep
```

## Проблемы?

### TUN интерфейс не создаётся

```bash
# Переустановить capabilities
sudo setcap 'cap_net_admin+ep' /opt/happ/tun/sing-box
sudo setcap 'cap_net_admin+ep' /opt/happ/core/xray

# Загрузить модуль tun
sudo modprobe tun
```

### Приложение не запускается

```bash
# Переустановить пакет
sudo pacman -R happ-desktop
makepkg -si --cleanbuild
```

## Удаление

```bash
sudo pacman -R happ-desktop
```

## Зависимости

Устанавливаются автоматически:
- `qt5-base`
- `qt5-svg`
- `qt5-declarative`
- `openssl`
- `libcap`

## Суть решения

**Проблема**: AppImage не может получить `cap_net_admin` для создания TUN интерфейса

**Решение**: Нативная установка с автоматической настройкой capabilities на VPN ядра

## Безопасность

✅ Только sing-box и xray получают `cap_net_admin`  
✅ GUI приложение работает без привилегий  
✅ Не используется sudo/suid  
✅ Минимальные права (только сетевые операции)  

## Структура установки

```
/opt/happ/
├── Happ                      # GUI (Qt5)
├── tun/sing-box             # ← cap_net_admin
├── core/xray                # ← cap_net_admin
└── antifilter/antifilter

/usr/bin/happ                 # Wrapper скрипт
```

## Полезные команды

```bash
# Информация о пакете
pacman -Qi happ-desktop

# Список файлов пакета
pacman -Ql happ-desktop

# Поиск файлов с capabilities
getcap -r /opt/happ/

# Проверка TUN модуля
lsmod | grep tun

# Проверка созданных интерфейсов
ip link show | grep tun
```

## Документация

- 📖 **README.md** - полное руководство
- 🔧 **BUILD.md** - сборка и отладка
- 🔒 **CAPABILITIES.md** - безопасность
- 💡 **SOLUTION.md** - техническое решение
- ⚡ **QUICKSTART.md** - этот файл

---

**Готово!** Приложение должно работать и создавать TUN интерфейсы.
